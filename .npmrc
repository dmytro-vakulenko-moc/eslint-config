# Several ESLint plugins (e.g. eslint-plugin-react) have not yet widened their
# peer range to declare ESLint 10 support, though they work with it. Accept the
# peer ranges in this dev environment; published consumers resolve their own deps.
legacy-peer-deps=true

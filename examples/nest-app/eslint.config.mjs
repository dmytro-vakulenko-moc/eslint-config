import { moc } from '@moc-global/eslint-config';

// Zero-config: the NestJS stack is auto-detected from @nestjs/core in this
// project's dependencies. Path aliases are read from tsconfig.json.
export default moc();
